#!/bin/bash
# Empty preset bootstrap script (idempotent).
# Called by create-project.sh with args:
#   PROJECT_DIR PROJECT_NAME DB_TYPE DB_HOST DB_NAME DB_USER DB_PASSWORD
set -e
echo "Empty preset: no additional files required."
